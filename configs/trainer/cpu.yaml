# CPU Trainer Configuration (for development and testing)

trainer:
  max_epochs: 100
  accelerator: cpu
  devices: 1
  precision: "32"
  gradient_clip_val: 1.0
  log_every_n_steps: 10
  enable_progress_bar: true
  enable_model_summary: true
